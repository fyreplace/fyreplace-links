{
  "name": "fyreplace-links",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare && make"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "nuxt": "^3.12.3",
    "vue-tsc": "^1.8.27"
  }
}
