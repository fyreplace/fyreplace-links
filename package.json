{
  "name": "fyreplace-links",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare && make"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "nuxt": "^3.20.1",
    "vue-tsc": "^3.1.4"
  }
}
