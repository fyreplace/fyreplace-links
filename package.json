{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare; make well-known"
  },
  "devDependencies": {
    "@types/node": "^18.15.0",
    "nuxt": "^3.2.3",
    "vue-tsc": "^1.2.0"
  }
}
