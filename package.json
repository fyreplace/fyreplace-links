{
  "name": "fyreplace-links",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare && make"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "nuxt": "^3.14.0",
    "vue-tsc": "^2.1.10"
  }
}
