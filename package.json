{
  "name": "fyreplace-links",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare && make"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "nuxt": "^3.12.4",
    "vue-tsc": "^2.0.29"
  }
}
